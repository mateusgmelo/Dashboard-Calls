# TradersLAB — Backend (Fastify + Prisma + SQLite + JWT)

## Rodar
```bash
cd backend
cp .env.example .env
npm i
npm run prisma:generate
npm run prisma:migrate
npm run seed
npm run dev
```
- API: http://localhost:3333
- Admin: admin@traderslab.local / admin1234
```